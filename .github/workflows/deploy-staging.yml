name: Deploy to Staging

on:
  push:
    branches: [staging]

jobs:
  deploy-migrations:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v5

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Link to staging project
        run: supabase link --project-ref ${{ secrets.SUPABASE_STAGING_PROJECT_REF }} --password ${{ secrets.SUPABASE_STAGING_DB_PASSWORD }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Push migrations to staging
        run: |
          for i in 1 2 3; do
            echo "Attempt $i..."
            supabase db push --include-all && exit 0
            echo "Attempt $i failed, waiting 60 seconds..."
            sleep 60
          done
          echo "All attempts failed — apply migrations locally with: supabase db push --include-all"
          exit 1
        timeout-minutes: 10
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_STAGING_DB_PASSWORD }}

  deploy-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm ci
      - run: npm run build

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy frontend to staging
        run: railway up --service workout-tracker
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

  deploy-api-proxy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: api-proxy/package-lock.json

      - run: npm ci
        working-directory: api-proxy
      - run: npm run build
        working-directory: api-proxy

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy api-proxy to staging
        run: railway up --service api-proxy
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
